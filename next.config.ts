/*
 * Next.js configuration for LeeMaster Design Hub.
 * PWA support will be added in a future phase.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
};

export default nextConfig;
