<!-- pipeline smoke test -->
This project uses a multi-agent pipeline orchestrator where a Tech Lead writes specs, an Engineer implements them, a Reviewer validates the diff, QA runs verification, and a Release agent opens the PR.
